function add( number1, number2 ) {
  return number1 + number2;
}

function sub( number1, number2 ) {
  return number1 - number2;
}

export default mathAbs( number1, number2 ) {
  return math.abs(number1, - number2);
}

export { add, sub };
